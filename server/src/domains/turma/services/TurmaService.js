// Configuração de todos os metodos que a entidade Turma precisa no sistema.

async function addTurma() {
    // Verifica se turma existe

    // Verifica se turma já não está cadastrada

    // Cadastra turma
    
}
